NAWA Production Starter

This is the NAWA source project. It is NOT an installable APK.
For Android/iPhone, the source must be built with Expo/EAS or native build tools.

Quick start:
1. Install Node.js.
2. Run: npm install
3. Run: npx expo start

Do not put secret Supabase/API keys inside the mobile app.
